source 'https://rubygems.org'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.1.4'

# Use Postgres for DB
gem 'pg'

# Run with speed.
gem 'puma'

# Rails.cache
gem 'redis-rails'

# Background jobs
gem 'resque'

gem 'pusher'

group :test do
  gem 'minitest', '5.4.0'
  gem 'guard-minitest'
end

group :development do
  gem 'pry-byebug'
end
