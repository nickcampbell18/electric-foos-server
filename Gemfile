source 'https://rubygems.org'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.1.4'

# Use Postgres for DB
gem 'pg'

# Run with speed.
gem 'puma'

group :test do
  gem 'minitest', '5.4.0'
  gem 'guard-minitest'
end

group :development do
  gem 'pry-byebug'
end
